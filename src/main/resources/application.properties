spring.datasource.url=${MA_DB_URL:jdbc:postgresql://localhost:5432/challenge}
spring.datasource.username=${MA_DB_USERNAME:test}
spring.datasource.password=${MA_DB_PASSWORD:test}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql = true

logging.level.root=info